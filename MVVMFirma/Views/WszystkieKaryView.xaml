<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieKaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="AntiqueWhite">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdKary}"/>
                <DataGridTextColumn Header="Imię czytelnika" Binding="{Binding CzytelnikImie}"/>
                <DataGridTextColumn Header="Nazwisko" Binding="{Binding CzytelnikNazwisko}"/>
                <DataGridTextColumn Header="Kwota" Binding="{Binding Kwota}"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Opis}"/>
                <DataGridTextColumn Header="Data naliczenia" Binding="{Binding DataNaliczenia}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
